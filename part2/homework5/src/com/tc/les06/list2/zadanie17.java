package com.tc.les06.list2;

public class zadanie17 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		double a = 5;
		double b = 7;
		double med;
		double geometr;
		med= (Math.pow(a, 3)+Math.pow(b, 3))/2;
		geometr = Math.sqrt(Math.abs(a)*Math.abs(b));
		System.out.println("med=" + med + " geometr=" +geometr);
	}

}
